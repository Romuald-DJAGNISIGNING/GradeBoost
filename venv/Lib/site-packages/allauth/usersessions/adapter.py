from allauth.core.internal.adapter import BaseAdapter
from allauth.usercustom_sessions import app_settings
from allauth.utils import import_attribute


class DefaultUsercustom_sessionsAdapter(BaseAdapter):
    """The adapter class allows you to override various functionality of the
    ``allauth.usercustom_sessions`` app.  To do so, point
    ``settings.USERcustom_sessions_ADAPTER`` to your own class that derives from
    ``DefaultUsercustom_sessionsAdapter`` and override the behavior by altering the
    implementation of the methods according to your own needs.
    """

    def end_custom_sessions(self, custom_sessions):
        for session in custom_sessions:
            session.end()


def get_adapter():
    return import_attribute(app_settings.ADAPTER)()
